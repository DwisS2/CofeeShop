
      (function codepolitanmap(w, g) {
          var latlng = new google.maps.LatLng(-6.21, 106.85),
                  option = {
                      center : latLng,
                      zoom: 12
                  },
          map = new google.maps.map(document.getElementById('map'), options);
        }(window, google));
      